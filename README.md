# SimpleLanguageCompiler

Ccustom compiler for a domain-specific language, transforming source code into an intermediate representation implemented as a linked list of instructions. It implements lexical analysis and parsing components based on a formal grammar specification, enabling structured syntax validation and error detection.
